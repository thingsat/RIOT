From 5d6728d50f5229f90fd582103a73c2409ac5a75f Mon Sep 17 00:00:00 2001
From: Francisco Molina <femolina@uc.cl>
Date: Fri, 8 Apr 2022 17:23:51 +0200
Subject: [PATCH 01/11] include/csp/csp_interface: move next pointer to
 begining of struct

---
 include/csp/csp_interface.h | 3 +--
 1 file changed, 1 insertion(+), 2 deletions(-)

diff --git a/include/csp/csp_interface.h b/include/csp/csp_interface.h
index 0d81d94..c61696d 100644
--- a/include/csp/csp_interface.h
+++ b/include/csp/csp_interface.h
@@ -12,7 +12,7 @@ typedef int (*nexthop_t)(csp_iface_t * iface, uint16_t via, csp_packet_t * packe
 
 /* This struct is referenced in documentation.  Update doc when you change this. */
 struct csp_iface_s {
-
+	struct csp_iface_s * next;  // Internal, interfaces are stored in a linked list
 	uint16_t addr;              // Host address on this subnet
 	uint16_t netmask;           // Subnet mask
 	const char * name;          // Name, max compare length is #CSP_IFLIST_NAME_MAX
@@ -31,7 +31,6 @@ struct csp_iface_s {
 	uint32_t txbytes;           // Transmitted bytes
 	uint32_t rxbytes;           // Received bytes
 	uint32_t irq;               // Interrupts
-	struct csp_iface_s * next;  // Internal, interfaces are stored in a linked list
 };
 
 /**
-- 
2.32.0

